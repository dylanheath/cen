type token = {
   XTZ: {
    name: string,
    image: string,
    contract: string | null,
  }
}

export const tokens: token = {
  XTZ: {
    name: "XTZ",
    image: "not found",
    contract: "not found",
  }
}
