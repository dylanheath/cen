import React, {useState, useEffect} from 'react';

export default function CurrencySelect() {
  const [setSelectedCurrency, SelectedCurrency] = useState<string>(); 

  return (
    <div>
    </div>
  )
}
